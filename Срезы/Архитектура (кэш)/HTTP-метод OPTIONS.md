## 1. Зачем нужен OPTIONS

Метод **OPTIONS** используется, чтобы **узнать, какие действия сервер разрешает** для конкретного URL.  
Проще говоря — клиент «спрашивает» сервер:

> «Какие методы (GET, POST, PUT и т.д.) и заголовки мне можно использовать при обращении к этому адресу?»

Это важно:

- для браузеров — чтобы решить, можно ли безопасно отправлять запрос с другого домена (CORS);
    
- для API-клиентов и инструментов вроде Postman — чтобы узнать, какие операции поддерживает endpoint.
    

---

## 2. Как это работает пошагово

### Шаг 1. Клиент готовится сделать запрос

Допустим, фронтенд на `https://frontend.com` хочет отправить `POST`-запрос на API `https://api.server.com`.

Так как домены разные, срабатывает **CORS**-механизм безопасности.

---

### Шаг 2. Браузер делает «предзапрос» (**Preflight Request**)

Перед настоящим запросом браузер **автоматически** отправляет **OPTIONS**-запрос:

```
OPTIONS https://api.server.com/data
Origin: https://frontend.com
Access-Control-Request-Method: POST
Access-Control-Request-Headers: Content-Type, Authorization
```

Он как бы спрашивает:

> «Можно ли мне отправить POST на этот URL и использовать такие-то заголовки?»

---

### Шаг 3. Сервер отвечает, что разрешено

Сервер должен вернуть специальные CORS-заголовки:

```
HTTP/1.1 204 No Content
Access-Control-Allow-Origin: https://frontend.com
Access-Control-Allow-Methods: GET, POST, OPTIONS
Access-Control-Allow-Headers: Content-Type, Authorization
Access-Control-Max-Age: 3600
```

Этот ответ говорит браузеру:

- с домена `frontend.com` можно обращаться;
    
- разрешены методы `GET`, `POST`, `OPTIONS`;
    
- разрешены указанные заголовки;
    
- результат можно кэшировать на 3600 секунд.
    

---

### Шаг 4. Браузер принимает решение

Если ответ сервера допустим — браузер отправляет **реальный POST-запрос**.  
Если нет (например, сервер не разрешил Origin или метод) — запрос блокируется, и вы видите ошибку CORS в консоли.

---

## 3. Когда OPTIONS вызывается автоматически

- При **нестандартных запросах** (например, метод ≠ GET/POST или есть кастомные заголовки).
    
- При **отправке JSON с Content-Type: application/json**.
    
- При **авторизации** через заголовок `Authorization`.
    

---

## 4. Пример на практике

Фронтенд:

```javascript
fetch("https://api.server.com/data", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ message: "hello" })
})
```

Перед этим запросом браузер выполнит `OPTIONS https://api.server.com/data`.  
Если сервер разрешит — тогда выполнится реальный `POST`.

---

## 🔹 5. Резюме

|Этап|Что делает|
|---|---|
|1|Клиент хочет отправить запрос|
|2|Браузер делает `OPTIONS`-предзапрос|
|3|Сервер отвечает, какие методы и заголовки разрешены|
|4|Если всё ок — выполняется основной запрос|
|5|Если нет — браузер блокирует запрос (ошибка CORS)|

---

Хочешь, я покажу пример, **как настроить обработку OPTIONS и CORS-policy в C# (ASP.NET Core)**?
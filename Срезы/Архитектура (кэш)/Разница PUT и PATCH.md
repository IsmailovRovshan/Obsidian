## Главное правило:

- **`PUT`** — **полная замена** ресурса.  
- **`PATCH`** — **частичное обновление** ресурса.

---

## Пример: Работа с пользователем

Допустим, у нас есть пользователь с ID `1`:

```json
{
  "id": 1,
  "name": "Алексей",
  "email": "alex@example.com",
  "age": 30,
  "city": "Москва"
}
```

---

### `PUT /users/1` — полная замена

Ты отправляешь **весь обновлённый объект**:

```json
PUT /users/1
{
  "name": "Александр",
  "email": "sasha@example.com",
  "age": 31,
  "city": "Санкт-Петербург"
}
```

**Что делает сервер?**  
Он **полностью заменяет** старый ресурс этим новым.  
Если ты **забудешь** указать, например, `city`, то в результате `city` **исчезнет**!

> 💥 Опасность `PUT`: если не передать поле — оно будет удалено (если сервер реализован по стандарту).

---

### `PATCH /users/1` — частичное обновление

Ты отправляешь **только то, что хочешь изменить**:

```json
PATCH /users/1
{
  "city": "Екатеринбург"
}
```

🔹 **Что делает сервер?**  
Он **обновит только поле `city`**, всё остальное (`name`, `email`, `age`) останется как было.

> ✅ Это удобно: не нужно знать всё о пользователе — только то, что меняешь.

---

## ⚖️ Сравнение в таблице

| Характеристика        | `PUT`                              | `PATCH`                              |
|----------------------|-------------------------------------|---------------------------------------|
| Тип операции         | Полная замена ресурса               | Частичное обновление                  |
| Что отправляешь?     | Весь обновлённый объект             | Только изменённые поля                |
| Что если пропустил поле? | Поле **удалится** (станет `null` или исчезнет) | Поле **останется без изменений** |
| Идемпотентность      | ✅ Да (всегда)                      | ✅ Должен быть (но зависит от реализации) |
| Когда использовать?  | Когда у тебя есть **полные данные** | Когда хочешь изменить **только часть** |

---

## Практический совет для C# разработчика

В ASP.NET Core:

- Для `PUT` принимай **полный DTO**:
  ```csharp
  [HttpPut("{id}")]
  public IActionResult UpdateUser(int id, UpdateUserDto dto)
  {
      // dto содержит ВСЕ поля
  }
  ```

- Для `PATCH` часто используют **JSON Patch** (стандарт RFC 6902):
  ```csharp
  [HttpPatch("{id}")]
  public IActionResult PartialUpdate(int id, JsonPatchDocument<User> patchDoc)
  {
      var user = GetUser(id);
      patchDoc.ApplyTo(user);
      Save(user);
  }
  ```

  Пример запроса:
  ```json
  PATCH /users/1
  [
    { "op": "replace", "path": "/city", "value": "Казань" }
  ]
  ```

---


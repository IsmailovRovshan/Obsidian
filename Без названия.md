# Получить
```sql
SELECT *
FROM NEWCONTACT_CYBER_EXERCISE_VIEW
```

```sql
select * from "Subjects"
```

```sql
select * from "Organizations"
```
# Удалить

```sql
delete from "Subjects";
delete from "Organizations"
```

```sql
DELETE FROM NEWCONTACT_CYBER_EXERCISE_VIEW
WHERE CITY = 'г. Череповец';
```


# Создать
```sql
INSERT INTO NEWCONTACT_CYBER_EXERCISE_VIEW 
(
    ACTIVE,
    FIO,
    PERSONNEL_NUMBER,
    PERSONNEL_NUMBER_OF_MANAGER,
    EMAIL,
    CITY,
    POSITION,
    DATE_OF_JOINING,
    DATE_OF_DISMISSAL,
    LEVEL1_BRANCH_NAME,
    BRANCH_CODE,
    LEVEL2_BRANCH_NAME,
    LEVEL2_CODE
)
VALUES 
(
    1,
    'Тестовыйй Тестовыйй Тестовыйй', 
    '54655544', 
    '12332512', 
    'sharaB1@example.com', 
    'г. Череповец', 
    'Аналитик', 
    TO_DATE('2025-07-30', 'YYYY-MM-DD'),
    NULL,
    'Предприятие нагрузочного тестирования первого уровня',
    '1:11111',
    'TestsI12',
    '200542016'
);
```